;==========================================================
; climax template: plain
;
; Black-and-white output. Inherits everything from
; `:climaxTemplate` (the base type's style primitives are
; identity, so output is plain text).
;==========================================================

import ./"base"!

define :plainTemplate is :climaxTemplate []
